import React from 'react';
import { Link } from "react-router-dom";
import { createPageUrl } from "@/utils";
import { Button } from "@/components/ui/button";
import { 
  FileText, 
  FileImage, 
  FileVideo, 
  Laptop, 
  Link as LinkIcon,
  ChevronRight,
  ArrowRight,
  Check
} from "lucide-react";

export default function FileConversionGuidePage() {
  // ... keep existing code (the entire content remains the same)
}